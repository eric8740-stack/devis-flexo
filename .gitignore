# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
backend/venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Variables d'environnement (secrets)
.env
.env.local
.env.*.local

# Base de données dev — règle GLOBALE (audit 05/07/2026 M8) : couvre aussi
# la db orpheline créée à la racine quand uvicorn/pytest est lancé du
# mauvais cwd (repo public, risque `git add -A`).
*.db
*.db-journal

# Uploads photos analyses (storage local dev, prod = Railway Volume)
backend/.uploads/

# Rapports moteur de coût (générés par pytest, régénérables à la demande)
backend/cost_breakdown.md
backend/cost_breakdown_5cas.md
backend/cost_breakdown_matching.md

# Next.js / Node
node_modules/
.next/
out/
build/
dist/
.vercel
next-env.d.ts
*.tsbuildinfo

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# Couverture de tests
.coverage
coverage/
htmlcov/
