fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sqlalchemy>=2.0.36
alembic>=1.14.0
psycopg2-binary>=2.9.10
pytest>=8.0.0
httpx>=0.27.0
pandas>=3.0.0
openpyxl>=3.1.0
# weasyprint 65.1 : Hotfix-PDF-C 01/05/2026, upgrade depuis 62.1 pour fixer
# AttributeError("'super' object has no attribute 'transform'") en Python
# 3.13.13 (CI Ubuntu). Bug d'incompatibilité weasyprint 62.x avec tinycss2
# récent — résolu dans les versions weasyprint 63.x+. Pin strict pour
# reproductibilité.
weasyprint==65.1
jinja2>=3.1.0
# Sprint 12 multi-tenant — auth JWT + email
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
# bcrypt pinné <5.0 : passlib 1.7.x est incompatible avec bcrypt 5.0+
# (changement d'API détection de version). Bug confirmé Sprint 12-A.
bcrypt>=4.0.0,<5.0
email-validator>=2.2.0
sendgrid>=6.11.0
